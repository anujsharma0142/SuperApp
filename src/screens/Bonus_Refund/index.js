import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const BonusRefund = () => {
  return (
    <View style={styles.container}>
      <Text>BonusRefund</Text>
    </View>
  );
};

export default BonusRefund;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
